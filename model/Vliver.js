class Vliver {
  constructor(fullName, color, avatarURL, channelURL) {
    this.fullName = fullName;
    this.color = color;
    this.avatarURL = avatarURL;
    this.channelURL = channelURL;
  }
}

module.exports = Vliver;
